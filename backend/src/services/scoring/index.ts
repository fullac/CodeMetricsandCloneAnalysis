export type { ScoreModelInput, ScoreModelResult } from "./types.js";

export function calculateScorePlaceholder(): never {
  throw new Error("Scoring model is reserved for a later implementation.");
}
